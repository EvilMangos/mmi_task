[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "notifier-telegram"
version = "0.1.0"
description = "Telegram bot client + message formatting"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/notifier_telegram"]
