[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "domain-orderbook"
version = "0.1.0"
description = "Pure types + top-10 aggregation rules"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/domain_orderbook"]
