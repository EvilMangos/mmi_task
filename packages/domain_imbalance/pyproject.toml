[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "domain-imbalance"
version = "0.1.0"
description = "Imbalance Ratio calculation (pure)"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/domain_imbalance"]
