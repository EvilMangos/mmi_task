[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "config"
version = "0.1.0"
description = "Config schema + loading (X, symbols, env)"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/config"]
